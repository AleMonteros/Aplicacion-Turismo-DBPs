<body class="pantallaHistoria">
    <div class="jumbotron bg-dark">
    <h1 class="display-3 text-white">Hello, world!</h1>
        <hr class="my-4">
        <p class="text-white">It uses utility classes for typography and spacing to space content out within the larger container.</p>
    </div>
</body>

<form action="@Url.Action("ContarH", "Historias")">
    <div class="form-group">
        <button type="submit" class="btn btn-primary btn-block bg-dark text-white">Añadir Historia</button>
    </div>
</form>

@model IEnumerable<Historia>

<div>
@foreach (var item in Model) 
{
    <div class="card card-signin my-5 text-white bg-dark border rounded">
    <div class="card-body text-white bg-dark">
        <h3 class="card-header border rounded text-center"> @Html.DisplayFor(modelItem => item.Titulo)</h3>
        <hr>
    <div class="card-body bg-dark text-white border rounded">
        <h6 class="card-subtitle mb-2 text-muted"> Historiador : @Html.DisplayFor(modelItem => item.Historiador)</h6>
        <p class="card-text"> @Html.DisplayFor(modelItem => item.Desarrollo)</p>
        <a class="btn btn-outline-primary" @Html.ActionLink("Continuar Leyendo", "MostrarH", new { id=item.ID })></a>
    </div>
    </div>
    </div>
}
</div>